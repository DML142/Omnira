# Placeholder template only. Copy to .env when runtime configuration is introduced.
# No service currently reads these proposed variables.
OMNIRA_ENV=local
OMNIRA_PROFILE=preview
OMNIRA_SYNTHETIC_DATA=true
OMNIRA_BILLING_ENABLED=false
OMNIRA_EMAIL_ENABLED=false
OMNIRA_SIMULATOR_ENABLED=false
